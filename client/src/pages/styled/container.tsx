import styled from '@emotion/styled';

const Container = styled.div`
  background-color: ${({ theme }) => theme.colors.N0};
  min-height: 100vh;
  min-width: 100vw;
  display: inline-flex;
`;

export { Container };
